1.Bruteforce, permutations перебирает все расстоновки от 0 до n-1, количество перестановок n!
в функции is_valid i и j пробегаются 2 раза по циклу , оба цикла выполняются за O(n), в сумме вся программа выполняется в худшем случае за O(n!*n^2)

2. Рекурсия is_safe проверяет только поставленных ферзей сложность О(row),рекуретное соотношение в худшем случаем проверяет все перестановки и дает О(n!) но из-за отсечения плохих ветвей на ранних этапах работает быстрее 
Итог: В худшем случае О(n!)

3.Симметрия, весь алгоритм такой же как в пункте 2 , но так-как мы проверяем только половину доски то работает в 2 раза быстрее для четных n и в 1,5 раза для нечетных n, Итоговая оценка: в худшем случае O(0.5*n!)